<section class="bg-warning">
    <nav aria-label="breadcrumb">
        <ol class="breadcrumb">
            {{#each navi.pages.href}}
            <li class="breadcrumb-item active">
                <a class="breadcrumb-item active text-success text-decoration-none" href="{{url}}">
                    {{name}}
                </a>
            </li>
            {{/each}}
        </ol>
    </nav>
</section>